apiVersion: v1
kind: Secret
metadata:
  name: {{ include "refmd.fullname" . }}
  labels:
    {{- include "refmd.labels" . | nindent 4 }}
type: Opaque
data:
  jwt-secret: {{ .Values.refmd.api.jwtSecret | b64enc | quote }}